trigger: none
schedules:
- cron: 2 0 * * *
  displayName: Midnight
  always: false
  branches:
    include:
    - master
stages:
- stage: Integration
  jobs:
  - deployment: SetupIntegration
    timeoutInMinutes: 180
    environment: test
    strategy:
      runOnce:
        deploy:
          steps:
          - download: none
          - checkout: self
          - task: NuGetAuthenticate@0
          - script: dotnet run -- SetupIntegration
            workingDirectory: ./src/Automatron.AzureDevOps.Sample
            displayName: Run Automatron
  - deployment: TeardownIntegration
    dependsOn:
    - SetupIntegration
    timeoutInMinutes: 180
    environment: test
    strategy:
      runOnce:
        deploy:
          steps:
          - download: none
          - checkout: self
          - task: NuGetAuthenticate@0
          - script: dotnet run -- TeardownIntegration
            workingDirectory: ./src/Automatron.AzureDevOps.Sample
            displayName: Run Automatron
